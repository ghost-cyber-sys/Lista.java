# Lista.java
📋 Lista de Inteiros em Java  Este projeto apresenta uma implementação simples de uma lista de inteiros em Java. A classe Lista armazena até 5 elementos, permitindo inserir valores e exibi-los no console.  🚀 Funcionalidades  Inserir elementos na lista  Exibir todos os elementos armazenados  Verificar se a lista está cheia antes de inserir

✅ Como funciona

Ao criar um objeto Lista, um vetor de tamanho 5 é inicializado.

O método inserir(int valor) adiciona um valor ao final da lista, se houver espaço.

O método mostrar() percorre todos os elementos inseridos e os exibe no console.

No método main, a lista é preenchida com cinco valores e exibida.

💻 Como executar

Salve o código em um arquivo chamado Lista.java.

Compile o programa no terminal/cmd:

javac Lista.java
java Lista

